apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: beszel-agent-worker
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: beszel-agent-worker
  template:
    metadata:
      labels:
        app: beszel-agent-worker
    spec:
      nodeSelector:
        worker: sourcream-worker
      hostNetwork: true
      containers:
      - name: agent
        image: henrygd/beszel-agent:latest
        ports:
        - containerPort: 45876
          hostPort: 45876
        env:
        - name: PORT
          value: "45876"
        - name: KEY
          value: "ssh-ed25519 BBBBD4..."  # From Beszel UI
        - name: TOKEN
          value: "your-worker-token"       # From Beszel UI
        - name: HUB_URL
          value: "http://x.x.x.x:30090"
        volumeMounts:
        - name: docker-sock
          mountPath: /var/run/docker.sock
          readOnly: true
      volumes:
      - name: docker-sock
        hostPath:
          path: /var/run/docker.sock