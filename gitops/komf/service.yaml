apiVersion: v1
kind: Service
metadata:
  name: komf
  namespace: media
  labels:
    app: komf
spec:
  type: ClusterIP           # reached via the Ingress (browser extension needs HTTPS — see ingress.yaml)
  selector:
    app: komf
  ports:
    - name: http
      port: 8085
      targetPort: http
