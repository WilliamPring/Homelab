apiVersion: v1
kind: Service
metadata:
  name: calibre
  namespace: media
  labels:
    app: calibre
spec:
  # type: ClusterIP
  type: NodePort
  selector:
    app: calibre
  ports:
    - port: 8083
      targetPort: 8083
      nodePort: 30083
